git clone URL
git status // conocer el estado del proyecto
git add -A // para añadir todos los cambios realizados
git remote add origin URL // si no tenemos asignado la direccion
git commit -m "mensaje" // realizamos el commit con el mensaje "mensaje"
git push origin master // subir los cambios al repositorio
git checkout -b branchName //crea y usa la rama branchName
git checkout branchName/master //te situa en la rama branchName/master
git gerge branchName // añade los cambios realizados en la rama master
